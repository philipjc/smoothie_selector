const TYPE_FORM = {
  name: 'type',
  values: ['fruit', 'vegetable', 'mixed']
};

const AMOUNT_FORM = {
  name: 'amount',
  values: [1, 5, 7]
};

const LIQUID_FORM = {
  name: 'liquid',
  values: ['water', 'milk', 'juice']
};

const EXTRAS_FORM = {
  name: 'extras',
  values: ['oats', 'honey', 'seeds']
};

export default { TYPE_FORM, AMOUNT_FORM, LIQUID_FORM, EXTRAS_FORM };
